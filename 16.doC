void main()
{
char str[10],c[10];
int i,j;
clrscr();
printf("input a string:");
scanf("%s",str);

for(i=0,j=0;str[i]!='\0';i++,j++)
{
c[i]=str[j];
}

printf("The copied string is %s",c);
getch();
}
