#include<conio.h>
#include<stdio.h>

int fact(int n) ;
void main()
{
int i;
clrscr();
printf("Enter a number");
scanf("%d",&i);
i=fact(i);
printf("%d",i);      ;
getch();
}

int fact(int n)
{
if(n>=1)
return (n*fact(n-1));
else
return 1;
}
