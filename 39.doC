#include<stdio.h>
#include<conio.h>
struct student{
int en;
char name[50];
int ps;
int age;
};
void main()
{
struct student st[5];
int i=0;
clrscr();
for(i=0;i<2;i++)
{
printf("Enter enrollment number of %d:\n",i+1);
scanf("%d",&st[i].en);
printf("Enter name of %d:\n",i+1);
scanf("%s",&st[i].name);
printf("Enter percentage of %d:\n",i+1);
scanf("%d",&st[i].ps);
printf("Enter age of %d:\n",i+1);
scanf("%d",&st[i].age);
}
for(i=0;i<2;i++)
{
printf("enrolment number of %d is %d\n",i+1,st[i].en);
printf("name of %d is %s\n",i+1,st[i].name);
printf("percentage of %d is%d\n",i+1,st[i].ps);
printf("age of %d is%d\n",i+1,st[i].age);
}
getch();
}

