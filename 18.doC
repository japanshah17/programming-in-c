void main()
{
char str[10],str1[10],str2[20];
int len=0,i,j;
clrscr();
printf("input a string:");
scanf("%s",str);

printf("input a string:");
scanf("%s",str1);

for(i=0;str[i]!='\0';i++)
{
str2[i]=str[i];
len++;
}

for(i=0,j=len;str1[i]!='\0';i++,j++)
{
str2[j]=str1[i];
}
printf("the concated string is %s",str2);
getch();
}