#include<stdio.h>
#include<conio.h>
void main()
{
char str[10];
int len=0,i;
clrscr();
printf("input a string:");
scanf("%s",str);
for(i=0;str[i]!='\0';i++)
{
len++;
}
printf("The length of string str is %d",len);
getch();
}
