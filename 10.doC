#include<conio.h>
#include<stdio.h>
void main()
{
int a[2][2],i,j,n;
clrscr();
 for(i=1;i<=2;i++)
 {
   for(j=1;j<=2;j++)
    {
    printf("enter element %d %d\n",i,j);
    scanf("%d",&a[i][j]);
    }
 }
 printf("enter element to search::");
 scanf("%d",&n);

  for(i=1;i<=2;i++)
   {
   for(j=1;j<=2;j++)
    {
     if(n==a[i][j])
     {
     printf("%d is present on %d %d\n",n,i,j);
     }
    }
   }
getch();
}